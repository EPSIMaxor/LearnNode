<!DOCTYPE <!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Learn Node</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="/assets/semanticui/semantic.min.css" />

    <style>
        .main.container{
            margin-top: 2em
        }

    </style>
</head>
<body>
    <h1><%= title %></h1>
    <div class="ui fixed inverted menu">
        <a href="/" class="header item">Accueil</a>
        <a href="/users" class="header item">Users</a>
        <a href="/ticket" class="header item">ticket</a>
        <div class="right menu">
            <a href="/" class="header item">Logout</a>
        </div>
    </div>
<div class="ui main container">
    <% if(locals.flash && locals.flash.error) {%>
    <div class="ui negative message">
        <%= flash.error %>
    </div>
    <% } %>

    <% if(locals.flash && locals.flash.user) {%>
        <div class="ui positive message">
            Bienvenue <%= flash.user %>
        </div>
        <% } %>